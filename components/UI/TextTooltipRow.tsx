import React from 'react'

const TextTooltipRow = ({ children }: { children: React.ReactNode }) => {
  return <div className="flex flex-row items-center">{children}</div>
}

export default TextTooltipRow
